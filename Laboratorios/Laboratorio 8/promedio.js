function calcularPromedio(numeros) {
    if (numeros.length === 0) {
        return 0;
    }
    let suma = 0;
    for (let i = 0; i < numeros.lengh; i++) {
        suma = suma + numeros[i];
    }
    let promedio = suma / numeros.length;
    return
}